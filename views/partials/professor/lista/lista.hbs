<div class="p-5">

    <div class="grid p-5">
        <form class="form-control">

            <div>
                <label class="form-label">Título: </label>
                <input type="text" class="form-control">
            </div>

            <div>
                <label class="form-label">Conteudo: </label>
                <textarea type="text" class="form-control"></textarea>
            </div>

            <div class="container">
                <input type="submit" class="btn btn-primary" value="Salvar">
                <input type="reset" class="btn btn-warning" value="Limpar">
            </div>

        </form>
    </div>

    <table id="myTable" class="display">
        <thead>
            <tr>
                <th>Titulo</th>
                <th>Conteudo</th>
                <th>Alterar</th>
                <th>Excluir</th>
            </tr>
        </thead>
        <tbody>
            {{#each people}}
            <tr>
                <td>{{titulo}}</td>
                <td>{{conteudo}}</td>
                <td><a class="btn btn-warning" onClick="alterar({{id}})">Alterar</a></td>
                <td><a class="btn btn-danger" onClick="excluir({{id}})">Excluir</a></td>
            </tr>
            {{/each}}
        </tbody>
    </table>

<script>
    $(document).ready(function() {
        $('#myTable').DataTable({
            "language": {
                "url": "https://cdn.datatables.net/plug-ins/1.11.5/i18n/Portuguese-Brasil.json"
            }
        });
    });

    function excluir(id){
        /*
        previamente um alerta 
        */

        Swal.fire({
            title: "Aviso!",
            text: "Você realmente deseja excluir este post?",
            icon: "warning"
        });

    }

</script>


</div>